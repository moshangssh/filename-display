# 循环依赖优化记录

## 第一阶段：了解和检测循环依赖 🔍

我们已成功完成了第一阶段工作：

1. **创建了依赖跟踪工具**

创建了 `DependencyTracker` 类，用于收集和可视化依赖关系：

```typescript
// src/utils/DependencyTracker.ts
export class DependencyTracker {
    private static dependencies: Map<string, string[]> = new Map();
    
    static addDependency(from: string, to: string): void {
        if (!this.dependencies.has(from)) {
            this.dependencies.set(from, []);
        }
        this.dependencies.get(from)?.push(to);
    }
    
    static logDependencies(): void {
        console.log("当前依赖关系图:");
        this.dependencies.forEach((deps, module) => {
            console.log(`${module} 依赖于: ${deps.join(', ')}`);
        });
    }
}
```

2. **在 FileProcessorService 和 FileDisplayCache 中添加了依赖跟踪**

在关键类的构造函数中添加了依赖跟踪代码，使我们能够清晰地了解各组件之间的依赖关系：

```typescript
// FileProcessorService.ts 中添加的代码
DependencyTracker.addDependency('FileProcessorService', 'FilenameParser');
if (fileDisplayCache) {
    DependencyTracker.addDependency('FileProcessorService', 'FileDisplayCache');
}
DependencyTracker.addDependency('FileProcessorService', 'ILoggerService');
DependencyTracker.addDependency('FileProcessorService', 'ITimerService');
DependencyTracker.addDependency('FileProcessorService', 'IPerformanceMonitor');
DependencyTracker.addDependency('FileProcessorService', 'IErrorHandler');
```

```typescript
// FileDisplayCache.ts 中添加的代码
DependencyTracker.addDependency('FileDisplayCache', 'ICacheStorage');
DependencyTracker.addDependency('FileDisplayCache', 'IElementAssociator');
DependencyTracker.addDependency('FileDisplayCache', 'ICacheMetadataManager');
DependencyTracker.addDependency('FileDisplayCache', 'IPersistenceManager');
DependencyTracker.addDependency('FileDisplayCache', 'ICacheWarmer');
DependencyTracker.addDependency('FileDisplayCache', 'ITitleExtractorPlugin');
DependencyTracker.addDependency('FileDisplayCache', 'ILoggerService');
DependencyTracker.addDependency('FileDisplayCache', 'ITimerService');
```

3. **在 Plugin 类中添加了依赖图可视化**

在 Plugin 的 onload 方法结束前添加了依赖图可视化，以便在插件加载时在控制台中查看依赖关系：

```typescript
// plugin.ts 中添加的代码
// 记录依赖关系
DependencyTracker.logDependencies();
```

## 第二阶段：简单接口抽象 📝

我们已成功完成了第二阶段工作：

1. **发现已有基本接口**

分析代码后发现项目中已经有了基本的接口定义：
- `IFileDisplayCache` 接口已定义在 `src/services/interfaces/IServices.ts` 文件中
- `FileDisplayCache` 类已经实现了该接口

2. **更新了引用，使用接口而非具体实现**

我们更新了代码，确保组件间依赖通过接口而不是具体实现：

```typescript
// FileProcessorService.ts 中的依赖跟踪代码修改
if (fileDisplayCache) {
    DependencyTracker.addDependency('FileProcessorService', 'IFileDisplayCache'); // 使用接口名称
}
```

```typescript
// ProgressiveCacheWarmer.ts 中的修改
import { ILoggerService, ITimerService, IFileProcessorService } from '../../interfaces/IServices';
// 移除了对具体实现的导入
// import { FileProcessorService } from '../../FileProcessorService';

constructor(
    private plugin: any,
    private logger: ILoggerService,
    private timerService: ITimerService,
    private fileProcessorService?: IFileProcessorService // 使用接口而非具体实现
) {}
```

```typescript
// FileDisplayCacheFactory.ts 中的修改
import { ILoggerService, ITimerService, IFileProcessorService } from '../interfaces/IServices';
// 移除了对具体实现的导入
// import { FileProcessorService } from '../FileProcessorService';

public static createFileDisplayCache(
    plugin: ITitleExtractorPlugin,
    loggerService: ILoggerService,
    timerService: ITimerService,
    fileProcessorService?: IFileProcessorService // 使用接口而非具体实现
): IFileDisplayCache {
    // ...
}
```

## 第三阶段：简单事件机制 🔄

我们已成功完成了第三阶段工作：

1. **创建了最小化事件总线**

创建了 `EventBus` 类，实现了简单的发布-订阅机制：

```typescript
// src/core/events/EventBus.ts
export class EventBus {
    private static instance: EventBus;
    private listeners: Map<string, Function[]> = new Map();
    
    static getInstance(): EventBus {
        if (!EventBus.instance) {
            EventBus.instance = new EventBus();
        }
        return EventBus.instance;
    }
    
    private constructor() {
        // 私有构造函数，防止直接实例化
    }
    
    subscribe(event: string, callback: Function): () => void {
        if (!this.listeners.has(event)) {
            this.listeners.set(event, []);
        }
        
        const callbacks = this.listeners.get(event)!;
        callbacks.push(callback);
        
        // 返回取消订阅的函数
        return () => {
            const index = callbacks.indexOf(callback);
            if (index !== -1) {
                callbacks.splice(index, 1);
            }
        };
    }
    
    publish(event: string, ...args: any[]): void {
        const callbacks = this.listeners.get(event) || [];
        callbacks.forEach(callback => {
            try {
                callback(...args);
            } catch (error) {
                console.error(`EventBus: 执行事件 ${event} 的回调时发生错误:`, error);
            }
        });
    }
}
```

2. **解耦了缓存未命中场景**

修改了 `FileDisplayCache.getDisplayName` 方法，使用事件总线发布缓存未命中事件：

```typescript
// FileDisplayCache.ts 中的修改
public getDisplayName(path: string): string | undefined {
    const item = this.cacheStorage.get(path);
    
    if (item) {
        // 更新访问时间和计数
        this.metadataManager.updateAccessTime(path);
        this.metadataManager.incrementAccessCount(path);
        
        return item.displayName;
    } else {
        // 通过EventBus发布缓存未命中事件，而不是直接调用处理服务
        // 尝试通过vault获取TFile对象
        const file = this.plugin.app.vault.getAbstractFileByPath(path);
        if (file instanceof TFile && EventBus.getInstance().hasSubscribers('cache:miss')) {
            EventBus.getInstance().publish('cache:miss', file);
            this.logger.debug(`文件 ${path} 的缓存未命中，已发布缓存未命中事件`);
        } else {
            this.logger.debug(`文件 ${path} 的缓存未命中，无法处理`);
        }
        return undefined;
    }
}
```

3. **在 Plugin 类中注册了事件监听**

在 `Plugin` 类中添加了对缓存未命中事件的监听处理：

```typescript
// Plugin.ts 中的修改
private registerEvents(): void {
    // ... 其他事件注册代码 ...
    
    // 使用EventBus注册缓存未命中事件处理
    const eventBus = EventBus.getInstance();
    eventBus.subscribe('cache:miss', async (file: TFile) => {
        this.loggerService.debug(`响应 cache:miss 事件：处理文件 ${file.path}`);
        try {
            // 因为processFile可能返回Promise<FileDisplayResult>或直接返回FileDisplayResult
            // 使用await确保无论返回类型是什么都能正确处理
            const result = await Promise.resolve(this.fileProcessorService.processFile(file));
            if (result && result.success && this.fileExplorerDisplayService) {
                await this.fileExplorerDisplayService.updateFileExplorerDisplay(file);
            }
        } catch (error: unknown) {
            this.loggerService.error(`处理文件 ${file.path} 时出错:`, error);
        }
    });
    
    // ... 其他事件注册代码 ...
}
```

通过这些修改，我们成功地解耦了 `FileDisplayCache` 和 `FileProcessorService` 之间的直接依赖关系，消除了循环依赖。现在，`FileDisplayCache` 不再直接依赖 `FileProcessorService`，而是通过事件机制间接通信。

## 第四阶段：简单服务定位器 🧩

我们已成功完成了第四阶段工作：

1. **利用已有服务容器**

我们发现项目中已有 `ServiceContainer` 类，它正是我们需要的服务定位器实现：

```typescript
// src/core/ServiceContainer.ts
export class ServiceContainer {
    private static instance: ServiceContainer;
    private services: Map<string, any> = new Map();
    private factories: Map<string, () => any> = new Map();
    
    private constructor() {}
    
    public static getInstance(): ServiceContainer {
        if (!ServiceContainer.instance) {
            ServiceContainer.instance = new ServiceContainer();
        }
        return ServiceContainer.instance;
    }
    
    public register<T>(key: string, instance: T): void {
        this.services.set(key, instance);
    }
    
    public get<T>(key: string): T {
        // 实现获取服务的逻辑
    }
    
    // 其他方法...
}
```

2. **在 FileProcessorService 中使用服务定位器**

我们在 `FileProcessorService` 类中添加了 `getFileDisplayCache` 方法，使用服务容器获取缓存服务：

```typescript
// FileProcessorService.ts 中添加的代码
protected getFileDisplayCache(): IFileDisplayCache | null {
    // 如果已经有缓存实例，直接使用
    if (this.fileDisplayCache) {
        return this.fileDisplayCache;
    }
    
    // 否则尝试从服务容器获取
    try {
        const container = ServiceContainer.getInstance();
        if (container.has('fileDisplayCache')) {
            return container.get<IFileDisplayCache>('fileDisplayCache');
        }
    } catch (error) {
        this.logger.error('从服务容器获取fileDisplayCache失败:', error);
    }
    
    return null;
}
```

3. **重写缓存相关方法**

修改了 `FileProcessorService` 中的 `cacheDisplayResult` 和 `getCachedDisplayName` 方法，使其使用服务定位器获取缓存服务：

```typescript
// FileProcessorService.ts 中修改的缓存结果方法
protected cacheDisplayResult(path: string, result: FileDisplayResult): void {
    if (result.success && result.displayName) {
        // 获取缓存服务
        const cache = this.getFileDisplayCache();
        if (cache) {
            cache.setDisplayName(path, result.displayName);
        }
    }
}

// FileProcessorService.ts 中修改的获取缓存方法
protected getCachedDisplayName(path: string): FileDisplayResult | null {
    // 获取缓存服务
    const cache = this.getFileDisplayCache();
    if (cache && cache.hasDisplayName(path)) {
        const cachedName = cache.getDisplayName(path);
        if (cachedName) {
            return { 
                success: true, 
                displayName: cachedName, 
                fromCache: true 
            };
        }
    }
    return null;
}
```

4. **添加异步处理包装器**

为了解决类型兼容性问题，我们在 `FileProcessorService` 类中添加了 `processFileWrapper` 方法：

```typescript
// FileProcessorService.ts 中添加的异步包装器方法
public processFileWrapper(file: TFile): Promise<FileDisplayResult> {
    return Promise.resolve().then(() => {
        // 直接调用基类的同步processFile方法
        const result = super.processFile(file);
        
        // 如果处理成功，更新文件名索引
        if (result.success) {
            this.fileNameIndexService.addFilesToQueue([file], true);
        }
        
        return result;
    });
}
```

5. **更新调用处**

我们更新了 `Plugin` 类中所有调用 `processFile` 的地方，改为调用 `processFileWrapper`：

```typescript
// Plugin.ts 中更新的代码
eventBus.subscribe('cache:miss', async (file: TFile) => {
    this.loggerService.debug(`响应 cache:miss 事件：处理文件 ${file.path}`);
    try {
        // 使用 processFileWrapper 而不是 processFile
        const result = await this.fileProcessorService.processFileWrapper(file);
        if (result && result.success && this.fileExplorerDisplayService) {
            await this.fileExplorerDisplayService.updateFileExplorerDisplay(file);
        }
    } catch (error: unknown) {
        this.loggerService.error(`处理文件 ${file.path} 时出错:`, error);
    }
});
```

通过这些修改，我们成功地利用服务定位器解决了 `FileProcessorService` 和 `FileDisplayCache` 之间的循环依赖问题。现在，`FileProcessorService` 不再直接依赖 `FileDisplayCache` 的具体实现，而是通过服务定位器按需获取，进一步降低了组件间的耦合度。

## 第五阶段：延迟初始化和获取 ⏱️

我们已成功完成了第五阶段工作：

1. **优化了 ProgressiveCacheWarmer 类**

修改了 `ProgressiveCacheWarmer` 类，添加了从服务容器获取文件处理服务的能力：

```typescript
// src/services/cache/warmer/ProgressiveCacheWarmer.ts
private getFileProcessorService(): IFileProcessorService | null {
    // 如果已有直接依赖的处理服务实例，优先使用
    if (this.fileProcessorService) {
        return this.fileProcessorService;
    }
    
    // 否则尝试从服务容器获取
    try {
        const container = ServiceContainer.getInstance();
        if (container.has('fileProcessorService')) {
            return container.get<IFileProcessorService>('fileProcessorService');
        }
    } catch (error) {
        this.logger.error('从服务容器获取fileProcessorService失败:', error);
    }
    
    return null;
}
```

同时优化了 `separateFilesByVisibility` 和 `processFilesBatch` 方法，使用延迟获取的处理服务：

```typescript
private separateFilesByVisibility(files: TFile[]): { visibleFiles: TFile[], otherFiles: TFile[] } {
    // 尝试从服务容器获取处理服务
    const processorService = this.getFileProcessorService();
    
    if (processorService) {
        // 如果获取到处理服务，使用其方法
        try {
            return processorService.separateFilesByVisibility(files);
        } catch (error) {
            this.logger.error('使用处理服务分离文件失败:', error);
            // 出错时回退到内部实现
        }
    }
    
    // 如果没有处理服务或处理服务调用失败，使用内部实现
    // ...内部实现代码...
}
```

2. **增强了 FileDisplayCacheFactory**

增加了 `getOrCreateFromContainer` 静态方法，支持从服务容器中延迟获取或创建缓存服务：

```typescript
// src/services/cache/FileDisplayCacheFactory.ts
public static getOrCreateFromContainer(
    plugin: ITitleExtractorPlugin
): IFileDisplayCache {
    const container = ServiceContainer.getInstance();
    
    // 如果容器中已存在缓存服务，直接返回
    if (container.has('fileDisplayCache')) {
        return container.get<IFileDisplayCache>('fileDisplayCache');
    }
    
    // 使用依赖解析器
    const resolver = new DependencyResolver();
    
    // 定义依赖项
    const dependencies = ['loggerService', 'timerService'];
    
    // 等待依赖项就绪后创建缓存
    resolver.whenReady(dependencies, (loggerService, timerService) => {
        if (!loggerService || !timerService) {
            throw new Error('创建FileDisplayCache失败: 缺少必要的依赖服务');
        }
        
        // 创建缓存并注册到容器
        const cache = this.createFileDisplayCache(plugin, loggerService, timerService);
        container.register('fileDisplayCache', cache);
        
        return cache;
    });
    
    // 注册参数化工厂函数
    container.registerParameterizedFactory('createFileDisplayCache', 
        (pluginInstance, logger, timer) => {
            return this.createFileDisplayCache(
                pluginInstance,
                logger,
                timer
            );
        }
    );
    
    // 其余逻辑...
}
```

同时修改了 `createFileDisplayCache` 方法，不再传递 `fileProcessorService` 参数给 `ProgressiveCacheWarmer` 构造函数。

3. **优化了 Plugin 类中的服务初始化**

使用工厂函数注册服务，实现延迟初始化：

```typescript
// src/plugin.ts
// 注册文件处理服务工厂函数 - 用于延迟解析
this.serviceContainer.registerFactory('fileProcessorService', () => {
    return this.fileProcessorService;
});

// 使用工厂方法初始化文件显示缓存 - 不传入处理服务（避免循环依赖）
this.fileDisplayCache = FileDisplayCacheFactory.createFileDisplayCache(
    this,
    this.loggerService,
    this.timerService
    // 不传入 fileProcessorService，避免循环依赖
);

// 注册缓存服务工厂函数 - 用于延迟解析
this.serviceContainer.registerFactory('fileDisplayCache', () => {
    return this.fileDisplayCache;
});
```

同时添加了 `updateServiceDependencies` 方法，在插件完全初始化后动态设置服务之间的依赖关系：

```typescript
private updateServiceDependencies(): void {
    this.loggerService.debug('更新服务依赖关系...');
    
    try {
        // 在FileExplorerDisplayService初始化后，更新文件处理服务的更新函数
        if (this.fileExplorerDisplayService) {
            const updateFileFn = async (file: TFile) => {
                return this.fileExplorerDisplayService?.updateFileExplorerDisplay(file);
            };
            this.fileProcessorService.setUpdateFileDisplayFn(updateFileFn);
        }
        
        // 注册EventBus事件处理
        const eventBus = EventBus.getInstance();
        eventBus.subscribe('cache:miss', async (file: TFile) => {
            this.loggerService.debug(`响应 cache:miss 事件：处理文件 ${file.path}`);
            try {
                // 使用 processFileWrapper 而不是 processFile
                const result = await this.fileProcessorService.processFileWrapper(file);
                if (result && result.success && this.fileExplorerDisplayService) {
                    await this.fileExplorerDisplayService.updateFileExplorerDisplay(file);
                }
            } catch (error: unknown) {
                this.loggerService.error(`处理文件 ${file.path} 时出错:`, error);
            }
        });
        
        // 记录依赖关系图，用于调试和监控
        DependencyTracker.logDependencies();
    } catch (error) {
        this.loggerService.error('更新服务依赖关系时出错:', error);
    }
}
```

4. **增强了 FileProcessorService 类的缓存机制**

优化了 `getFileDisplayCache` 方法，不仅从服务容器获取缓存服务，还在本地缓存获取结果，提高后续访问性能：

```typescript
// src/services/FileProcessorService.ts
protected getFileDisplayCache(): IFileDisplayCache | null {
    // 如果已经有缓存实例，直接使用
    if (this.fileDisplayCache) {
        return this.fileDisplayCache;
    }
    
    // 否则尝试从服务容器获取
    try {
        const container = ServiceContainer.getInstance();
        if (container.has('fileDisplayCache')) {
            const cache = container.get<IFileDisplayCache>('fileDisplayCache');
            // 缓存获取到的实例以提高后续访问性能
            this.fileDisplayCache = cache;
            return cache;
        }
    } catch (error) {
        this.logger.error('从服务容器获取fileDisplayCache失败:', error);
    }
    
    return null;
}
```

5. **完善了 EventBus 类**

添加了 `hasSubscribers` 方法，用于检查事件是否有订阅者，避免不必要的事件发布：

```typescript
// src/core/events/EventBus.ts
/**
 * 检查是否有指定事件的订阅者
 * @param event 事件名称
 * @returns 是否存在订阅者
 */
hasSubscribers(event: string): boolean {
    const callbacks = this.listeners.get(event);
    return !!callbacks && callbacks.length > 0;
}
```

通过这些改进，我们成功地实现了延迟初始化和获取机制，解决了 `FileProcessorService` 和 `FileDisplayCache` 之间的循环依赖问题。现在，服务只在实际需要时才被创建和获取，避免了初始化时的循环依赖问题，同时提高了系统的性能和稳定性。

## 第六阶段：轻量级依赖注册和解析 💉

我们已成功完成了第六阶段工作：

1. **扩展了服务容器支持参数化工厂和回调**

增强了 `ServiceContainer` 类，添加了参数化工厂和依赖回调支持：

```typescript
// src/core/ServiceContainer.ts 的扩展
export class ServiceContainer {
    // 现有代码...
    private parameterizedFactories: Map<string, (...args: any[]) => any> = new Map();
    private dependencyCallbacks: Map<string, Function[]> = new Map();
    private pendingResolves: Set<string> = new Set();
    
    // 注册带参数的工厂函数
    public registerParameterizedFactory<T>(key: string, factory: (...args: any[]) => T): void {
        this.parameterizedFactories.set(key, factory);
    }
    
    // 添加服务解析回调
    public onResolve<T>(key: string, callback: (service: T) => void): void {
        if (!this.dependencyCallbacks.has(key)) {
            this.dependencyCallbacks.set(key, []);
        }
        
        const callbacks = this.dependencyCallbacks.get(key)!;
        callbacks.push(callback);
        
        // 如果服务已经存在，立即调用回调
        if (this.services.has(key)) {
            callback(this.services.get(key));
        }
    }
    
    // 循环依赖检测
    public get<T>(key: string): T {
        // 检测循环依赖
        if (this.pendingResolves.has(key)) {
            throw new Error(`检测到循环依赖: ${Array.from(this.pendingResolves).join(' -> ')} -> ${key}`);
        }
        
        // ... 现有实现 ...
    }
    
    // 使用参数化工厂创建服务
    public createWithParams<T>(key: string, ...args: any[]): T {
        if (this.parameterizedFactories.has(key)) {
            const factory = this.parameterizedFactories.get(key)!;
            return factory(...args);
        }
        
        throw new Error(`参数化工厂未注册: ${key}`);
    }
}
```

2. **创建了依赖解析器**

创建了 `DependencyResolver` 类，辅助自动化解析依赖关系：

```typescript
// src/core/DependencyResolver.ts
export class DependencyResolver {
    private serviceContainer: ServiceContainer;
    
    constructor() {
        this.serviceContainer = ServiceContainer.getInstance();
    }
    
    // 解析依赖项
    public resolveDependencies(dependencies: string[]): any[] {
        return dependencies.map(dep => {
            if (this.serviceContainer.has(dep)) {
                return this.serviceContainer.get(dep);
            }
            return undefined;
        });
    }
    
    // 注册带依赖的服务
    public registerWithDependencies<T>(
        serviceKey: string,
        dependencies: string[],
        factory: (...deps: any[]) => T
    ): void {
        this.serviceContainer.registerFactory(serviceKey, () => {
            const resolvedDeps = this.resolveDependencies(dependencies);
            return factory(...resolvedDeps);
        });
    }
    
    // 等待依赖就绪
    public whenReady(dependencies: string[], callback: (...services: any[]) => void): void {
        let resolvedCount = 0;
        const resolved: any[] = new Array(dependencies.length).fill(undefined);
        
        dependencies.forEach((dep, index) => {
            // 如果服务已存在，立即记录
            if (this.serviceContainer.has(dep) && this.serviceContainer.get(dep)) {
                resolved[index] = this.serviceContainer.get(dep);
                resolvedCount++;
                
                // 如果所有依赖都解析完毕，执行回调
                if (resolvedCount === dependencies.length) {
                    callback(...resolved);
                }
                return;
            }
            
            // 否则注册回调等待服务就绪
            this.serviceContainer.onResolve(dep, (service) => {
                resolved[index] = service;
                resolvedCount++;
                
                // 如果所有依赖都解析完毕，执行回调
                if (resolvedCount === dependencies.length) {
                    callback(...resolved);
                }
            });
        });
    }
}
```

3. **改进了 FileDisplayCacheFactory**

使用依赖解析器优化了 `FileDisplayCacheFactory`：

```typescript
// src/services/cache/FileDisplayCacheFactory.ts
public static getOrCreateFromContainer(
    plugin: ITitleExtractorPlugin
): IFileDisplayCache {
    const container = ServiceContainer.getInstance();
    
    // 如果容器中已存在缓存服务，直接返回
    if (container.has('fileDisplayCache')) {
        return container.get<IFileDisplayCache>('fileDisplayCache');
    }
    
    // 使用依赖解析器
    const resolver = new DependencyResolver();
    
    // 定义依赖项
    const dependencies = ['loggerService', 'timerService'];
    
    // 等待依赖项就绪后创建缓存
    resolver.whenReady(dependencies, (loggerService, timerService) => {
        if (!loggerService || !timerService) {
            throw new Error('创建FileDisplayCache失败: 缺少必要的依赖服务');
        }
        
        // 创建缓存并注册到容器
        const cache = this.createFileDisplayCache(plugin, loggerService, timerService);
        container.register('fileDisplayCache', cache);
        
        return cache;
    });
    
    // 注册参数化工厂函数
    container.registerParameterizedFactory('createFileDisplayCache', 
        (pluginInstance, logger, timer) => {
            return this.createFileDisplayCache(
                pluginInstance,
                logger,
                timer
            );
        }
    );
    
    // 其余逻辑...
}
```

4. **优化了 ProgressiveCacheWarmer**

增强了 `ProgressiveCacheWarmer` 以使用依赖解析器：

```typescript
// src/services/cache/warmer/ProgressiveCacheWarmer.ts
constructor(
    private plugin: ITitleExtractorPlugin,
    private logger: ILoggerService,
    private timerService: ITimerService,
    fileProcessorService?: IFileProcessorService
) {
    if (fileProcessorService) {
        this.fileProcessorService = fileProcessorService;
    }
    
    // 添加依赖跟踪
    DependencyTracker.addDependency('ProgressiveCacheWarmer', 'ITitleExtractorPlugin');
    DependencyTracker.addDependency('ProgressiveCacheWarmer', 'ILoggerService');
    DependencyTracker.addDependency('ProgressiveCacheWarmer', 'ITimerService');
    if (fileProcessorService) {
        DependencyTracker.addDependency('ProgressiveCacheWarmer', 'IFileProcessorService');
    }
    
    // 初始化依赖解析器
    this.resolver = new DependencyResolver();
    
    // 设置依赖就绪回调
    if (!fileProcessorService) {
        this.resolver.whenReady(['fileProcessorService'], (service) => {
            this.fileProcessorService = service;
            this.logger.debug('ProgressiveCacheWarmer: 通过依赖解析器获取到文件处理服务');
        });
    }
}
```

5. **重构了插件初始化逻辑**

重构了 `Plugin.initServices` 方法，使用依赖解析器注册服务：

```typescript
// src/plugin.ts
private initServices(): void {
    // 初始化服务容器
    this.serviceContainer.clear();
    
    // 创建依赖解析器
    const resolver = new DependencyResolver();
    
    // 初始化基础服务并注册
    this.loggerService = new LoggerService();
    this.timerService = new TimerService(this);
    this.serviceContainer.register('plugin', this);
    this.serviceContainer.register('loggerService', this.loggerService);
    this.serviceContainer.register('timerService', this.timerService);
    
    // 使用参数化工厂注册日志服务
    this.serviceContainer.registerParameterizedFactory('createLogger', (name: string) => {
        return this.loggerService.getLogger(name);
    });
    
    // 使用依赖解析器注册带依赖的服务
    resolver.registerWithDependencies(
        'fileProcessorService',
        ['plugin', 'filenameParser', 'loggerService', 'timerService', 'performanceMonitor', 'errorHandler'],
        (plugin, filenameParser, loggerService, timerService, perfMonitor, errHandler) => {
            // 创建并返回服务
            this.fileProcessorService = new FileProcessorService(
                plugin, filenameParser, undefined, loggerService,
                timerService, perfMonitor, errHandler
            );
            return this.fileProcessorService;
        }
    );
    
    // 使用依赖解析器注册文件显示缓存
    resolver.registerWithDependencies(
        'fileDisplayCache', 
        ['plugin', 'loggerService', 'timerService'],
        (plugin, loggerService, timerService) => {
            this.fileDisplayCache = FileDisplayCacheFactory.createFileDisplayCache(
                plugin, loggerService, timerService
            );
            return this.fileDisplayCache;
        }
    );
    
    // 等待关键服务初始化完成后设置依赖关系
    resolver.whenReady(
        ['fileProcessorService', 'fileDisplayCache'], 
        (processorService, displayCache) => {
            // 设置回调和初始化相互依赖
            const updateFileFn = async (file: TFile) => {
                return this.fileExplorerDisplayService?.updateFileExplorerDisplay(file);
            };
            processorService.setUpdateFileDisplayFn(updateFileFn);
        }
    );
}
```

6. **添加了服务依赖关系更新方法**

添加了 `updateServiceDependencies` 方法，在插件完全初始化后动态更新依赖关系：

```typescript
// src/plugin.ts
private updateServiceDependencies(): void {
    try {
        this.loggerService.debug('更新服务依赖关系...');
        
        // 创建依赖解析器
        const resolver = new DependencyResolver();
        
        // 等待关键服务解析完成后处理回调
        resolver.whenReady(
            ['fileProcessorService', 'fileDisplayCache', 'fileExplorerDisplayService'], 
            (processorService, displayCache, displayService) => {
                // 设置或更新服务回调函数
                if (processorService && displayService) {
                    const updateFileFn = async (file: TFile) => {
                        return displayService.updateFileExplorerDisplay(file);
                    };
                    processorService.setUpdateFileDisplayFn(updateFileFn);
                    this.loggerService.debug('已更新FileProcessorService的文件更新回调');
                }
            }
        );
        
        // 确保事件管理服务正确运行
        if (this.eventManager) {
            // 重新设置事件监听器
            this.eventManager.setupVaultEventListeners();
            this.eventManager.setupMetadataEventListeners();
            this.loggerService.debug('已重新设置事件监听器');
        }
        
        // 记录服务依赖状态
        DependencyTracker.logDependencies();
    } catch (error) {
        this.loggerService.error('更新服务依赖关系时出错:', error);
    }
}
```

通过这些改进，我们成功实现了更加强大和灵活的依赖注入系统，使得服务间的依赖关系更加清晰、解耦，并且能够自动化解析依赖，彻底解决了循环依赖问题。服务组件现在可以通过声明式依赖获取所需的其他服务，同时系统能够检测并防止循环依赖的发生。

## 下一步计划 📋

我们将继续按照渐进式解决方案的步骤进行优化：

1. **第七阶段：改造插件主类**
   - 通过服务容器初始化所有服务
   - 进一步分离插件核心逻辑和服务管理
   - 实现完全的依赖倒置原则
   - 添加服务生命周期管理

后续阶段将根据进展情况继续实施。 🚀 