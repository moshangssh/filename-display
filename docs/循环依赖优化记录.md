# 循环依赖优化记录

## 第一阶段：了解和检测循环依赖 🔍

我们已成功完成了第一阶段工作：

1. **创建了依赖跟踪工具**

创建了 `DependencyTracker` 类，用于收集和可视化依赖关系：

```typescript
// src/utils/DependencyTracker.ts
export class DependencyTracker {
    private static dependencies: Map<string, string[]> = new Map();
    
    static addDependency(from: string, to: string): void {
        if (!this.dependencies.has(from)) {
            this.dependencies.set(from, []);
        }
        this.dependencies.get(from)?.push(to);
    }
    
    static logDependencies(): void {
        console.log("当前依赖关系图:");
        this.dependencies.forEach((deps, module) => {
            console.log(`${module} 依赖于: ${deps.join(', ')}`);
        });
    }
}
```

2. **在 FileProcessorService 和 FileDisplayCache 中添加了依赖跟踪**

在关键类的构造函数中添加了依赖跟踪代码，使我们能够清晰地了解各组件之间的依赖关系：

```typescript
// FileProcessorService.ts 中添加的代码
DependencyTracker.addDependency('FileProcessorService', 'FilenameParser');
if (fileDisplayCache) {
    DependencyTracker.addDependency('FileProcessorService', 'FileDisplayCache');
}
DependencyTracker.addDependency('FileProcessorService', 'ILoggerService');
DependencyTracker.addDependency('FileProcessorService', 'ITimerService');
DependencyTracker.addDependency('FileProcessorService', 'IPerformanceMonitor');
DependencyTracker.addDependency('FileProcessorService', 'IErrorHandler');
```

```typescript
// FileDisplayCache.ts 中添加的代码
DependencyTracker.addDependency('FileDisplayCache', 'ICacheStorage');
DependencyTracker.addDependency('FileDisplayCache', 'IElementAssociator');
DependencyTracker.addDependency('FileDisplayCache', 'ICacheMetadataManager');
DependencyTracker.addDependency('FileDisplayCache', 'IPersistenceManager');
DependencyTracker.addDependency('FileDisplayCache', 'ICacheWarmer');
DependencyTracker.addDependency('FileDisplayCache', 'ITitleExtractorPlugin');
DependencyTracker.addDependency('FileDisplayCache', 'ILoggerService');
DependencyTracker.addDependency('FileDisplayCache', 'ITimerService');
```

3. **在 Plugin 类中添加了依赖图可视化**

在 Plugin 的 onload 方法结束前添加了依赖图可视化，以便在插件加载时在控制台中查看依赖关系：

```typescript
// plugin.ts 中添加的代码
// 记录依赖关系
DependencyTracker.logDependencies();
```

## 第二阶段：简单接口抽象 📝

我们已成功完成了第二阶段工作：

1. **发现已有基本接口**

分析代码后发现项目中已经有了基本的接口定义：
- `IFileDisplayCache` 接口已定义在 `src/services/interfaces/IServices.ts` 文件中
- `FileDisplayCache` 类已经实现了该接口

2. **更新了引用，使用接口而非具体实现**

我们更新了代码，确保组件间依赖通过接口而不是具体实现：

```typescript
// FileProcessorService.ts 中的依赖跟踪代码修改
if (fileDisplayCache) {
    DependencyTracker.addDependency('FileProcessorService', 'IFileDisplayCache'); // 使用接口名称
}
```

```typescript
// ProgressiveCacheWarmer.ts 中的修改
import { ILoggerService, ITimerService, IFileProcessorService } from '../../interfaces/IServices';
// 移除了对具体实现的导入
// import { FileProcessorService } from '../../FileProcessorService';

constructor(
    private plugin: any,
    private logger: ILoggerService,
    private timerService: ITimerService,
    private fileProcessorService?: IFileProcessorService // 使用接口而非具体实现
) {}
```

```typescript
// FileDisplayCacheFactory.ts 中的修改
import { ILoggerService, ITimerService, IFileProcessorService } from '../interfaces/IServices';
// 移除了对具体实现的导入
// import { FileProcessorService } from '../FileProcessorService';

public static createFileDisplayCache(
    plugin: ITitleExtractorPlugin,
    loggerService: ILoggerService,
    timerService: ITimerService,
    fileProcessorService?: IFileProcessorService // 使用接口而非具体实现
): IFileDisplayCache {
    // ...
}
```

## 第三阶段：简单事件机制 🔄

我们已成功完成了第三阶段工作：

1. **创建了最小化事件总线**

创建了 `EventBus` 类，实现了简单的发布-订阅机制：

```typescript
// src/core/events/EventBus.ts
export class EventBus {
    private static instance: EventBus;
    private listeners: Map<string, Function[]> = new Map();
    
    static getInstance(): EventBus {
        if (!EventBus.instance) {
            EventBus.instance = new EventBus();
        }
        return EventBus.instance;
    }
    
    private constructor() {
        // 私有构造函数，防止直接实例化
    }
    
    subscribe(event: string, callback: Function): () => void {
        if (!this.listeners.has(event)) {
            this.listeners.set(event, []);
        }
        
        const callbacks = this.listeners.get(event)!;
        callbacks.push(callback);
        
        // 返回取消订阅的函数
        return () => {
            const index = callbacks.indexOf(callback);
            if (index !== -1) {
                callbacks.splice(index, 1);
            }
        };
    }
    
    publish(event: string, ...args: any[]): void {
        const callbacks = this.listeners.get(event) || [];
        callbacks.forEach(callback => {
            try {
                callback(...args);
            } catch (error) {
                console.error(`EventBus: 执行事件 ${event} 的回调时发生错误:`, error);
            }
        });
    }
}
```

2. **解耦了缓存未命中场景**

修改了 `FileDisplayCache.getDisplayName` 方法，使用事件总线发布缓存未命中事件：

```typescript
// FileDisplayCache.ts 中的修改
public getDisplayName(path: string): string | undefined {
    const item = this.cacheStorage.get(path);
    
    if (item) {
        // 更新访问时间和计数
        this.metadataManager.updateAccessTime(path);
        this.metadataManager.incrementAccessCount(path);
        
        return item.displayName;
    } else {
        // 通过EventBus发布缓存未命中事件，而不是直接调用处理服务
        // 尝试通过vault获取TFile对象
        const file = this.plugin.app.vault.getAbstractFileByPath(path);
        if (file instanceof TFile && EventBus.getInstance().hasSubscribers('cache:miss')) {
            EventBus.getInstance().publish('cache:miss', file);
            this.logger.debug(`文件 ${path} 的缓存未命中，已发布缓存未命中事件`);
        } else {
            this.logger.debug(`文件 ${path} 的缓存未命中，无法处理`);
        }
        return undefined;
    }
}
```

3. **在 Plugin 类中注册了事件监听**

在 `Plugin` 类中添加了对缓存未命中事件的监听处理：

```typescript
// Plugin.ts 中的修改
private registerEvents(): void {
    // ... 其他事件注册代码 ...
    
    // 使用EventBus注册缓存未命中事件处理
    const eventBus = EventBus.getInstance();
    eventBus.subscribe('cache:miss', async (file: TFile) => {
        this.loggerService.debug(`响应 cache:miss 事件：处理文件 ${file.path}`);
        try {
            // 因为processFile可能返回Promise<FileDisplayResult>或直接返回FileDisplayResult
            // 使用await确保无论返回类型是什么都能正确处理
            const result = await Promise.resolve(this.fileProcessorService.processFile(file));
            if (result && result.success && this.fileExplorerDisplayService) {
                await this.fileExplorerDisplayService.updateFileExplorerDisplay(file);
            }
        } catch (error: unknown) {
            this.loggerService.error(`处理文件 ${file.path} 时出错:`, error);
        }
    });
    
    // ... 其他事件注册代码 ...
}
```

通过这些修改，我们成功地解耦了 `FileDisplayCache` 和 `FileProcessorService` 之间的直接依赖关系，消除了循环依赖。现在，`FileDisplayCache` 不再直接依赖 `FileProcessorService`，而是通过事件机制间接通信。

## 下一步计划 📋

我们将继续按照渐进式解决方案的步骤进行优化：

1. **第四阶段：简单服务定位器**
   - 创建基础服务定位器
   - 在插件初始化时注册服务

2. **第五阶段：延迟初始化和获取**
   - 实现服务的延迟获取
   - 修改组件以支持延迟依赖获取

后续阶段将根据进展情况继续实施。 